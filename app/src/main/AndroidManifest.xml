<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jia.callhelper"
    android:versionCode="14"
    android:versionName="1.6">

    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="34"/>

    <!-- 弹出自己的全屏来电通知（锁屏时点亮屏幕） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <!-- 安卓 10+ 后台弹出大按钮界面所需 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!--
        安卓 11+（targetSdk >= 30）的「包可见性」限制：应用默认看不到其它应用。
        必须显式声明才能：
        1) 看到微信（无障碍在微信界面里找「接听」按钮）
        2) 看到系统「文字转语音」引擎 —— 否则本应用连不上语音引擎，
           会在小米等机型上误报「手机没有安装语音引擎」，语音播报直接失效。
    -->
    <queries>
        <package android:name="com.tencent.mm"/>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/AppTheme">

        <!-- 首页（启动器入口）：只展示家人联系人 -->
        <activity android:name=".HomeActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 设置页：从首页「⚙ 设置」进入 -->
        <activity android:name=".SettingsActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden"/>

        <!-- 添加家人 -->
        <activity android:name=".AddContactActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"/>

        <!-- 家人详情 / 编辑 / 删除 -->
        <activity android:name=".ContactDetailActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"/>

        <!-- 全屏大按钮来电界面：锁屏之上显示、亮屏、常亮 -->
        <activity android:name=".CallAlertActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/AlertTheme"/>

        <!-- 通知监听：读取微信来电通知（需用户在系统设置里授权） -->
        <service android:name=".WeChatCallListenerService"
            android:exported="true"
            android:label="@string/listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>

        <!-- 无障碍服务：在微信通话界面里自动点「接听/挂断」（需用户在系统设置里授权） -->
        <service android:name=".CallHelperAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>
    </application>
</manifest>
